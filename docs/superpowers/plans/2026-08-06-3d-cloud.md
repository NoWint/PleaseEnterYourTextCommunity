# 3D 球状词云 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把总结看板左侧 sd-canvas 的 2D 词云替换为 Canvas 2D 手写 3D 可拖拽球状词云（拖拽惯性 + 自动慢转 + 点击显词频）。

**Architecture:** 新组件 `src/components/cloudSphere.ts` 导出 `mountCloudSphere(canvas, words)`——Fibonacci 球面分布 + 旋转矩阵投影 + rAF 渲染循环 + pointer 事件（拖拽/惯性/点击）+ reduced-motion 降级。summaryDashboard 的 `drawCloudAsync` 改为调它。

**Tech Stack:** Vanilla TS, Canvas 2D, requestAnimationFrame, Pointer Events。

**Spec:** `docs/superpowers/specs/2026-08-06-3d-cloud-design.md`

---

### Task 1: cloudSphere.ts — 3D 球组件

**Files:**
- Create: `src/components/cloudSphere.ts`

- [ ] **Step 1: 写组件**

```ts
// 3D 球状词云:Canvas 2D 手写投影(零依赖)。
// Fibonacci 球面分布 + 透视(近大远小/前亮后暗) + 拖拽惯性 + 自动慢转 + 点击显词频。
import type { WordFreq } from '../utils/wordAnalysis.js';

const COLORS = ['#4a90d9', '#e06c6c', '#4caf50', '#d9a441', '#8e6cd9', '#2aa0a0', '#e08a3c', '#6ca0e0'];
const REDUCED = window.matchMedia?.('(prefers-reduced-motion: reduce)').matches ?? false;

interface Point { x: number; y: number; z: number; word: string; w: number; }

/** 返回销毁函数(清理事件 + rAF)。 */
export function mountCloudSphere(canvas: HTMLCanvasElement, words: WordFreq[]): () => void {
  if (words.length < 3) return () => {}; // 词太少 → 静默空
  const dpr = window.devicePixelRatio || 1;
  const rect = canvas.getBoundingClientRect();
  const W = rect.width || 240, H = rect.height || 150;
  canvas.width = W * dpr;
  canvas.height = H * dpr;
  const ctx = canvas.getContext('2d');
  if (!ctx) return () => {};
  ctx.scale(dpr, dpr);

  // Fibonacci 球面:均匀分布,避免极点堆积
  const R = Math.min(W, H) * 0.42;
  const n = words.length;
  const pts: Point[] = words.map((w, i) => {
    const t = (i + 0.5) / n;
    const phi = Math.acos(1 - 2 * t);
    const theta = Math.PI * (1 + Math.sqrt(5)) * i;
    return { x: R * Math.sin(phi) * Math.cos(theta), y: R * Math.cos(phi), z: R * Math.sin(phi) * Math.sin(theta), word: w.word, w: w.weight };
  });
  const maxW = Math.max(...words.map((w) => w.weight), 1);

  let rotY = 0, rotX = 0.35;        // 初始俯仰
  let vx = 0, vy = 0;               // 惯性速度
  let autoSpin = !REDUCED;          // 自动慢转(需非 reduced)
  let dragging = false;
  let lastX = 0, lastY = 0;
  let hx = 0, hy = 0;               // 命中检测坐标(供点击)
  let lastT = performance.now();
  let raf = 0;

  const proj = (p: Point): { sx: number; sy: number; scale: number; z: number } => {
    // 绕 Y 再绕 X
    const cx = Math.cos(rotX), sx = Math.sin(rotX);
    const cy = Math.cos(rotY), sy = Math.sin(rotY);
    const x1 = p.x * cy + p.z * sy;
    const z1 = -p.x * sy + p.z * cy;
    const y2 = p.y * cx - z1 * sx;
    const z2 = p.y * sx + z1 * cx;
    const persp = R * 2;
    const s = persp / (persp + z2);
    return { sx: W / 2 + x1 * s, sy: H / 2 + y2 * s, scale: s, z: z2 };
  };

  const render = () => {
    ctx.clearRect(0, 0, W, H);
    // 按 z 排序:近的在上面
    const sorted = pts.map((p) => ({ p, q: proj(p) })).sort((a, b) => b.q.z - a.q.z);
    for (const { p, q } of sorted) {
      const alpha = 0.25 + 0.75 * (q.z + R) / (2 * R); // 前亮后暗
      const size = 11 + 17 * Math.sqrt(p.w / maxW) * q.scale;
      const color = COLORS[Math.abs(p.word.charCodeAt(0)) % COLORS.length];
      ctx.globalAlpha = alpha;
      ctx.font = `600 ${Math.max(8, size)}px system-ui, sans-serif`;
      ctx.fillStyle = color;
      ctx.textAlign = 'center';
      ctx.textBaseline = 'middle';
      ctx.fillText(p.word, q.sx, q.sy);
    }
    ctx.globalAlpha = 1;
  };

  const tick = () => {
    const now = performance.now();
    const dt = Math.min(50, now - lastT);
    lastT = now;
    if (!dragging) {
      if (autoSpin) rotY += 0.003 * (dt / 16.7);
      // 惯性衰减
      if (Math.abs(vx) > 0.0001 || Math.abs(vy) > 0.0001) {
        rotX += vx * dt;
        rotY += vy * dt;
        vx *= 0.9; vy *= 0.9;
      }
    }
    render();
    raf = requestAnimationFrame(tick);
  };

  // 命中检测:最近 z>0 的词
  const hit = (mx: number, my: number): Point | null => {
    let best: Point | null = null, bd = 40;
    for (const p of pts) {
      const q = proj(p);
      if (q.z <= 0) continue;
      const d = Math.hypot(mx - q.sx, my - q.sy);
      if (d < bd) { bd = d; best = p; }
    }
    return best;
  };

  // 点击显示词频 tooltip
  const onClick = (e: MouseEvent) => {
    const r = canvas.getBoundingClientRect();
    const p = hit(e.clientX - r.left, e.clientY - r.top);
    if (!p) return;
    const f = words.find((w) => w.word === p.word);
    canvas.title = `${p.word} · ${f ? f.count : ''} 次`;
  };
  canvas.addEventListener('click', onClick);

  // 拖拽 + 惯性
  const onDown = (e: PointerEvent) => {
    dragging = true; autoSpin = false;
    lastX = e.clientX; lastY = e.clientY;
    hx = 0; hy = 0;
    canvas.setPointerCapture(e.pointerId);
  };
  const onMove = (e: PointerEvent) => {
    if (!dragging) return;
    const dx = e.clientX - lastX, dy = e.clientY - lastY;
    rotY += dx * 0.005; rotX += dy * 0.005;
    vx = dy * 0.005; vy = dx * 0.005;
    lastX = e.clientX; lastY = e.clientY;
  };
  const end = () => { dragging = false; };
  canvas.addEventListener('pointerdown', onDown);
  canvas.addEventListener('pointermove', onMove);
  canvas.addEventListener('pointerup', end);
  canvas.addEventListener('pointercancel', end);

  tick();
  return () => {
    cancelAnimationFrame(raf);
    canvas.removeEventListener('click', onClick);
    canvas.removeEventListener('pointerdown', onDown);
    canvas.removeEventListener('pointermove', onMove);
    canvas.removeEventListener('pointerup', end);
    canvas.removeEventListener('pointercancel', end);
  };
}
```

- [ ] **Step 2: Type-check**

Run: `npx tsc --noEmit`
Expected: 无输出(通过)

- [ ] **Step 3: 提交**

```bash
git add src/components/cloudSphere.ts
git commit -m "feat(summary): 3D 球状词云组件(Fibonacci球面+透视+拖拽惯性+自动慢转)"
```

---

### Task 2: summaryDashboard 接入

**Files:**
- Modify: `src/components/summaryDashboard.ts` (drawCloudAsync)

- [ ] **Step 1: 改 drawCloudAsync**

把 `drawCloudAsync` 里对 `drawWordCloud` 的调用改为 `mountCloudSphere`:

```ts
async function drawCloudAsync(canvas: HTMLCanvasElement, msgs: MsgDto[], resolve: (t: string) => string): Promise<void> {
  try {
    const { initSegmenter, computeTopWords } = await import('../utils/wordAnalysis.js');
    const { mountCloudSphere } = await import('./cloudSphere.js');
    await initSegmenter(); // 幂等单例
    const words = computeTopWords(msgs, resolve, 14);
    if (words.length === 0) return;
    mountCloudSphere(canvas, words);
  } catch (err) {
    console.warn('[summary-cloud] 3D 词云绘制失败:', err);
  }
}
```

移除对 `drawWordCloud` 的引用(它在 wordCloud.ts,不再用于看板;词频弹窗 openWordAnalysisPopup 仍用它,不影响)。

- [ ] **Step 2: 调整 sd-canvas 尺寸**

看板左侧导航 sd-canvas 目前 `width="240" height="150"`(summaryDashboard.ts:295)。球状词云需要更大更方区域。改为 `width="210" height="190"`(更接近方形,球更舒展):

```ts
<canvas class="sd-canvas" width="210" height="190"></canvas>
```

- [ ] **Step 3: Type-check + build**

Run: `npx tsc --noEmit && npx vite build`
Expected: 通过

- [ ] **Step 4: 提交**

```bash
git add src/components/summaryDashboard.ts
git commit -m "feat(summary): 看板 sd-canvas 接入 3D 球状词云 + 尺寸调方"
```

---

### Task 3: 验证

**Files:** 无新增

- [ ] **Step 1: 浏览器验证**

Run: 打开看板(浏览器 mock 会因 jieba 失败,但验证组件本身):
- `mountCloudSphere` 对 mock 词数据渲染球(词分布、近大远小)。
- 拖拽 → 旋转;松手 → 惯性衰减。
- 自动慢转(非 reduced-motion)。
- 点击词 → tooltip 词频。

若 jieba 在浏览器不可用,用 node 验证几何逻辑(Fibonacci 分布 + 投影),或临时传硬编码词列表给 mountCloudSphere 测试渲染。

- [ ] **Step 2: 提交(如有修复)**

```bash
git add -A
git commit -m "fix(summary): 3D 词云验证修复"
```

---

## Self-Review

**Spec 覆盖检查:**
- §2.1 数据(computeTopWords Top14)→ Task 1/2 ✓
- §2.2 Fibonacci 球面 → Task 1 ✓
- §2.3 透视投影(近大远小/前亮后暗)→ Task 1 ✓
- §2.4 拖拽/惯性/自动慢转/点击词频 → Task 1 ✓
- §2.5 rAF 渲染循环 → Task 1 ✓
- §2.6 reduced-motion 降级 → Task 1(REDUCED)✓
- §3 新组件 cloudSphere.ts + drawCloudAsync 接入 → Task 2 ✓
- §4 错误处理(词<3 静默/分词失败)→ Task 1/2 ✓

**Type 一致性:** `mountCloudSphere(canvas, words: WordFreq[])` Task 1 定义、Task 2 调用。`WordFreq` 从 wordAnalysis import。`drawCloudAsync` 保留原签名(msgs/resolve)Task 2 内改实现。

**占位符检查:** 无 TBD;每步含代码/命令。✓

**注意:** Task 1 的 cleanup 里 `canvas.removeEventListener('click', null)` 无效(需保存 handler 引用)。实现时应保存所有 handler 引用并在销毁时移除。此为 Task 1 自审需修正点——实现时用命名 handler 变量。
